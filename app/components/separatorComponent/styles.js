import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  separatorLine : {
    borderBottomColor : '#9f9fa3',
    borderBottomWidth : 1,
    overflow : "hidden",
  }
})

export default styles;
